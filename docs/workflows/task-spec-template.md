# Task Spec: {编号}-{简短名称}

> 用于实现任务拆解的共享模板。
> 配合 `docs/workflows/multi-agent-collaboration.md` 使用。

> 由 Opus (Architect) 填写。写得越具体，Coding Model 跑偏概率越低。
> 填完后删除本行及所有 `{ }` 占位符。

---

## 基本信息

| 字段 | 值 |
|------|-----|
| **Spec 编号** | {如: PROJ-A-001, FEAT-002} |
| **创建者** | Opus (Architect) |
| **执行模型** | {DeepSeek / Sonnet / Haiku} |
| **依赖** | {前置 Spec 编号，无则写"无"} |
| **预计改动文件数** | {N} |
| **难度** | {low / medium / high} |

---

## 任务目标

{一句话，从用户/产品视角描述。}

---

## 上下文背景

### 为什么做
{业务价值、用户痛点、性能问题}

### 关联功能
{属于哪个大功能？关联哪些模块？}

### 当前状态
{现在怎么实现的？哪里不够好？}

---

## 涉及文件（精确）

| 操作 | 文件路径（从项目根开始） | 改动说明 |
|------|-------------------------|---------|
| 新建 | `{path/to/new/file}` | {负责什么} |
| 修改 | `{path/to/existing/file}` | {改什么，为什么} |
| 不改 | `{path/to/DO_NOT_TOUCH}` | {为什么不能碰——提醒 Coding Model} |

---

## 架构约束

> 违反任何一条 = REWORK。

### 必须遵守的模式
- {如：数据流必须是 Controller → Service → Repository，不可跳过 Service}
- {如：所有 API 响应必须使用统一信封格式 `{ success, data, error }`}
- {如：做好不可变数据，禁止 mutate 入参}

### 需对齐的现有模式
- {如：错误处理方式与 `src/utils/errors.js` 一致}
- {如：命名约定与项目现有代码一致}

---

## 需参考的现有代码

> Coding Model 必须先读这些文件，对齐风格后再写代码。

| 文件 | 参考目的 |
|------|---------|
| `{path/to/similar}` | {类似实现，参照其模式} |
| `{path/to/utils}` | {工具函数的导出/使用方式} |

---

## 实现指引

### 推荐方案
{简要描述技术方案，给明确方向}

### 关键步骤
1. {步骤1}
2. {步骤2}
3. {步骤3}

### 边界情况（必须处理）
- {输入为空/异常时}
- {网络/IO 失败时}
- {极端数据/并发时}

---

## 测试要求

| 测试文件 | 测什么 |
|---------|---------|
| `{path/to/__tests__/xxx.test}` | {核心逻辑} |
| `{同上}` | {边界情况} |

- 核心逻辑覆盖率 ≥ 80%
- 必须覆盖的边界：{列举}

---

## 验收标准（可量化）

- [ ] {可量化标准1}
- [ ] {可量化标准2}
- [ ] 编译/构建通过
- [ ] 所有测试通过
- [ ] 无 lint 错误

---

## 风险提示

| 风险 | 影响 | 规避方式 |
|------|------|---------|
| {容易出错点} | {后果} | {怎么避免} |
| {模块交互点} | {冲突可能} | {协调方式} |

---

## 禁止事项

> Coding Model 的边界线。明确不能做的事。

- {如：不要修改 xxx 模块}
- {如：不要引入新的第三方依赖}
- {如：不要改变现有 API 签名}
- {如：不要在本文件范围内做"顺手重构"}

---

## 完成后：Coding Model 汇报模板

```markdown
## 完成报告: {Spec 编号}

### 改动摘要
| 文件 | 操作 | 行数 |
|------|------|------|
| | | |

### 测试结果
- 单元测试: N pass / 0 fail
- 覆盖率: XX%
- 构建: 通过/失败

### 自检
- [ ] 函数 < 50 行
- [ ] 文件 < 800 行
- [ ] 无硬编码密钥
- [ ] 无深层嵌套 (>4层)
- [ ] 遵循命名约定
- [ ] 未修改 Spec 范围外文件

### 注意事项
{需要 Opus 审查时特别注意的点}
```

---

## 审查记录

| 轮次 | 日期 | 审查人 | 结论 | 备注 |
|------|------|--------|------|------|
| | | | PASS / REWORK / BLOCK | |
